import React from 'react';

const Footer = () => (
  <footer style={{padding:'1rem', textAlign:'center'}}>
    <small>© {new Date().getFullYear()} Molor</small>
  </footer>
);

export default Footer;